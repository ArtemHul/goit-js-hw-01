function speed(distance, time) {
    const maxSpeed =parseFloat (distance / time);

    return `the maximum speed of your car - ${maxSpeed} km/hourse `
}
console.log (speed (300, 2.5));
console.log (speed (800, 4.6));

//********************************/